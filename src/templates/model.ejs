import _ from 'lodash';

export default class <%=_name%> {
    <% for(var i = 0; i < _properties.length; ++i) {%>
    private _<%=_properties[i].name%>: <%=_properties[i].type%>;
    <% } %>
    constructor(data: any) {
<% for(var i = 0; i < _properties.length; ++i) {%>
        this._<%=_properties[i].name%> = data.<%=_properties[i].name%>;
<% } %>
    }

    toJSON(): any {
        return {
<% for(var i = 0; i < _properties.length; ++i) {%>
            <%=_properties[i].name%>: this._<%=_properties[i].name%>,
<% } %>
        }
    }
    <% for(var i = 0; i < _properties.length; ++i) {%>
    get <%=_properties[i].name%>(): <%=_properties[i].type%> {
        return this._<%=_properties[i].name%>;
    }
    <% } %>
    <% for(var i = 0; i < _properties.length; ++i) {%>
    set <%=_properties[i].name%>(value: <%=_properties[i].type%>) {
        this._<%=_properties[i].name%> = value; 
    }
    <% } %>
}