// ==== [GENERATED]
import {AxiosAction, createActionsObject, createHttpRequest, composeUrlWithParameters} from '../utils/utils.ts';
import {API, GET, POST, PUT, UPDATE, DELETE} from '../utils/constants.ts';
<%var pathI; var methodI; var method%>
// URLs
<%for(pathI = 0; pathI < _paths.length; ++pathI) {for(methodI = 0; methodI < _paths[pathI]._methods.length; ++methodI) {
    method = _paths[pathI]._methods[methodI]%>
const api<%=method._type%><%=method._name%>URL = '<%=_paths[pathI]._path%>';
<% }} %>
// Variables for reducers actions
<%for(pathI = 0; pathI < _paths.length; ++pathI) {for(methodI = 0; methodI < _paths[pathI]._methods.length; ++methodI) {
    method = _paths[pathI]._methods[methodI]%>
export const api<%=method._type%><%=method._name%>Responses = createActionsObject('api<%=method._type%><%=method._name%>Responses');
<%}}%>
// Paths to endpoint methods
<% for(pathI = 0; pathI < _paths.length; ++pathI) {for(methodI = 0; methodI < _paths[pathI]._methods.length; ++methodI) {
    method = _paths[pathI]._methods[methodI]%>
export default const api<%=method._type%><%=method._name%>Action = (<%=method._requestBody?'data: '+method._requestBody._name+', ':''%>callback?: any) =>
    createHttpRequest(api<%=method._type%><%=method._name%>Responses, <%=method._type.toUpperCase()%>, api<%=method._type%><%=method._name%>URL, <%=method._requestBody?'data':'null'%>, callback);
<% }} %>